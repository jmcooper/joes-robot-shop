import { TestBed } from '@angular/core/testing';
import { AppComponent } from './app.component';

describe('fakes', () => {
  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });

  it('1', () => {
    expect(1).toBe(1);
  });
});
